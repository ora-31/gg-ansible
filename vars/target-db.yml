---
host:
  hostname:            dev-cloud87.dbaas.oraclecloud.com
  instance_name:       OCIDEV1
  env_script:          ~/oradev.env
  dump_directory:      /home/oracle/dumps
  ora_dump_directory:  DBA_DIR
  run_directory:       /tmp

gg_config:
  username:            gg_admin
  password:            gg_admin

...
